[package]
name = "cho"
version = "0.2.0"
edition = "2024"
description = "A tiny semantic awk for filtering and transforming line-oriented data"
license = "MIT"
repository = "https://github.com/k0f1sh/cho"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["std"] }
chrono-tz = "0.10.4"
csv = "1.3"
ipnet = "2.11"
regex = "1.11"
semver = "1.0"
url = "2.5"
