# TODO

## バイナリ配布を始めるとき

- 直接依存と推移依存を含むすべての第三者ライセンスを再確認し、
  `THIRD_PARTY_LICENSES`または`THIRD_PARTY_NOTICES`を生成して配布物へ同梱する。

## 優先して対応するもの

### 式の構文深度を無制限にする

現在は再帰処理によるスタックオーバーフローを防ぐため、式のリスト構文を256段までに
制限している。パーサーだけでなく、ASTから実行形式へのコンパイル、評価、深いASTの
破棄も明示的なヒープ上の作業スタックを使う反復処理へ変更し、安全に深度制限を撤廃する。

単にパーサーだけを反復処理へ変えると、後段へスタックオーバーフローを移すだけなので、
構文の受理から実行と破棄までを一体として設計する。

## 利用例から見えた機能候補

### 文字列を局所的な入力レコードとして扱う式（仮称 `with-input`）

設計案・未実装。名前と構文は仮であり、以下の例は現行バイナリでは実行できない。

多段choでは、前段の出力を次段の入力として既存の式を再利用できる。同じ発想で、
値として得た文字列を一つの式の評価中だけ入力レコードとして扱えるようにする。
部分文字列に対しても、フィールド参照、範囲指定、型付き関数、結合をそのまま使い、
評価結果を外側の式へ値として返す。

`s/part`は区切った要素を一つ取り出す用途に適している。一方、同じ文字列から複数の
要素を取り出して並べ替えたり加工したりすると、分割の指定を繰り返すことになる。
局所的な入力レコードを作れば、一度の分割から既存の式を組み合わせて処理できる。
`s/part`の置き換えや廃止を前提にはしない。

構文のたたき台：

```lisp
(with-input TEXT BODY)
(with-input TEXT SEPARATOR BODY)
```

引数順、区切り方の表現、既定の分割規則は未確定。以下では、SEPARATORを文字列の
完全一致による区切りと仮定する。正規表現による`-F`と同じ意味とは限らない。

```lisp
; 外側の入力: job42 ready api:worker:8080
; 結果: job42 worker:8080
(p $1 (with-input $3 ":" (s/join ":" $2 $3)))

; 内側のフィールドもStringであり、関数の要求に応じてNumberへ変換する
; 結果: 8081
(with-input $3 ":" (+ $3 1))
```

評価規則の案：

- TEXTとSEPARATORは外側の入力コンテキストで一度だけ評価する。
- BODYは通常の再帰的な式として解析する。プログラム文字列を再解析する仕組みにはしない。
- BODYの評価中だけ、`$0`をTEXT、`$1`以降と`NF`を局所的な分割結果にする。
  存在しないフィールドは既存の規則と同じく空文字とする。
- BODYは一つの値を返し、その型を保って外側へ渡す。自動出力は行わない。
  `print`や`filter`を内側で実行する構文にはせず、外側の`print`や`filter`の
  引数として合成する。
- 入れ子では直近の入力コンテキストを参照し、評価後は外側へ戻る。
  任意の変数束縛、代入、レコードをまたぐ状態やループは導入しない。
- `NR`と実行時エラーのレコード番号は元の入力ストリームの番号を維持する案とする。
  エラーは握りつぶさず、既存の関数名・引数位置・期待型を含む診断と非0終了を保つ。

決める必要がある事項：

- 短く意図の伝わる名前と、分割設定が読み取りやすい構文。
- 既定の分割を空白にするか外側から継承するか。CLIの`-F`、`--csv`、`--tsv`が
  内側へ意図せず影響しない規則にする。
- 完全一致と正規表現の区切りをどこまで扱うか。`s/part`の空要素を残す性質と
  既存のフィールド分割との差を明示し、空の区切りやゼロ幅一致の扱いも決める。
- TEXTが空文字、存在しないフィールド、文字列以外の値の場合の挙動。
  空入力時の`NF`、連続する区切り、先頭・末尾の空要素を定義する。
- 改行を含むTEXTも単一の局所レコードとするか。CSV/TSVの局所解析は初期実装に
  必要かを検討し、複数レコードの反復処理へは広げない。
- 範囲指定のセパレータ保存規則。CSVを扱う場合は既存の範囲指定の制約との整合。
- `$1`の参照先が入れ子で変わる読みづらさ。外側の値との組み合わせが必要な実例で、
  任意の変数束縛や外側参照専用構文を増やさずに十分かを確認する。
- 局所レコード内の失敗箇所を診断へどう補足するか。

実装時は専用のAST表現とコンパイル・評価処理を設け、入力コンテキストを局所的に
渡す特殊形式として扱う。通常の関数と同じように引数を先にすべて評価してはならない。
引数不足・過剰、内側の不正な構文、空入力、区切りの境界条件、入れ子、外側の参照の
復帰、型付きの戻り値、エラー診断をテストする。helpには未確定点を解消した仕様と
コピーして試せる例を載せ、`s/part`、多段choそれぞれの用途との整合を確認する。

### Dateの書式化

`examples/daily-export-path.sh`では、前日の日付を求めたあと、文字列置換と結合で
`2026/08/31/sales.csv`というパスを作っている。`dt/fmt`と同じ値・書式の引数順で
Dateを直接書式化する`d/fmt`を追加すると、日付からパスを作る意図を明確に表せる。

構文案（未実装）：

```lisp
(d/fmt DATE FORMAT)
```

パイプラインの末尾を`cho -c d/fmt '%Y/%m/%d/sales.csv'`にできるようにする。
Dateはタイムゾーンを持たず、DateTimeとの暗黙変換も行わない。
日付に使える書式指定と、時刻・タイムゾーンの指定や不正な書式を拒否する規則を決める。

## 実例が集まったら検討するもの

### Durationの任意表示

Durationはそのまま出力すれば秒数になる。人向けの表示が本当に必要な実例を集めてから、
`du/fmt`の書式を決める。

`examples/find-long-jobs.sh`は用途の一例。現在の`elapsed=45min`に加え、
秒まで含む実行時間を`1h 23m 10s`のように表示できると、運用レポートで読みやすい。
総分数の表示は既存の`du/to-m`と`str`で表現できるため、時分秒への分解が必要な
実例を集める。引数順は`dt/fmt`や`-c`での連鎖と揃え、値を先に置く案とする。

構文案（未実装）：

```lisp
(du/fmt (dt/diff $1 $2) FORMAT)
```

決める必要がある事項：

- 時分秒の各要素と総時間をどう区別するか
- 24時間を超える期間と負の期間の表示
- 小数秒の精度と末尾ゼロ
- FORMATを自由な書式文字列にするか、少数の名前付き形式にするか

### その他の型付き値

ログや運用データによく現れ、文字列のままでは正しく扱いにくい値を候補とする。
汎用的なデータ加工機能を増やすのではなく、値の意味を保ったまま短い式で扱えることを
choの強みにする。

優先して検討する候補：

- MACアドレス：表記揺れを吸収した等値比較

各型は、次を満たすか検討してから一つずつ実装する。

- 文字列比較では意味的に誤る、またはシェルだけで正しく扱うのが難しい
- 検証だけでなく、比較、抽出、変換など複数の有用な操作を定義できる
- 文脈変換によって記述が明確に短くなる
- 外部I/Oや変更可能なグローバル状態を必要とせず、値として合成できる
- 型名、関数名、既定表示、失敗時の診断を`cho --help`で把握できる

各型について、正規化、既定表示、比較、失敗時の診断、最小の型固有操作を決める。
文字列の見た目から型を自動判定せず、`bs/`、`semver/`、`d/`、`url/`など各
名前空間の関数が期待型を決める現在の規則を維持する。

## 計測してから検討するもの

### レコード内の型変換キャッシュ

同じ値をURL、DateTime、IPアドレス、CIDR、SemVerなどへ複数回変換するプログラムでは、
現在は各操作がそれぞれ文字列を解析する。実測で型変換の負荷が問題になった場合は、
プログラムのコンパイル時に同じ値への同じ型変換が複数あるか判定し、必要な場合だけ
キャッシュスロットを割り当てる。

```lisp
(print (url/host $1) (url/path $1) (url/port $1))
```

キャッシュはレコード単位で作成してレコード処理後に廃棄する。同一の値を表すASTだけを
共有対象とし、同じ型変換を一度しか行わないプログラムにはキャッシュを確保しない。
特定の型だけの仕組みにせず、型変換の共通基盤として設計する。
