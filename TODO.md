# 型付き値と日時値の実装計画

## 目的

choの実行時の値を型付きにし、明示的な数値変換と日時そのものを表す値を追加する。
異なる型を暗黙に比較せず、入力ミスを黙って通さない。日時のパースと表示はchoが
担当し、並べ替えは既存の`sort`へ委ねる。

```console
$ cho '(print (dt $1) $2)' | sort -k1,1
```

日時値をそのまま出力した場合は、辞書順でも時系列順になるUTCのRFC 3339形式にする。
任意の表示形式が必要な場合だけ、明示的なフォーマット式を使う。

## 構文

RFC 3339から日時値を作る基本の式は、短く`dt`とする。その他の日時操作には`dt/`を
接頭辞として付ける。将来、URIやJSONなどの型付きの値を追加する場合も、基本の生成は
`uri`や`json`、追加操作は`uri/...`や`json/...`のように同じ規則で名前を付けられる
ようにする。
各式は`cho --help`の名前と短い例だけで用途と引数を理解でき、そのままワンライナーへ
使えることを命名とhelp記述の基準にする。

```lisp
(dt VALUE)               ; RFC 3339文字列から日時値を作る
(dt/unix VALUE)          ; Unix timestamp（秒）から日時値を作る
(dt/fmt FORMAT VALUE)    ; 日時値またはRFC 3339文字列を任意形式へ変換する
```

## 厳密な型比較と数値変換

比較では暗黙の型変換を行わず、同じ型の値だけを比較する。入力フィールドは文字列値
なので、数値として比較する場合は`num`で明示的に数値へ変換する。

```lisp
(num VALUE)
```

```lisp
(filter (> (num $2) 20))
```

- 文字列同士は、数値のように見える文字列も含めて文字列として比較する。
- 数値同士は数値として比較する。
- 日時値同士は後述の規則で日時として比較する。
- 文字列、数値、日時値の異なる型を比較した場合は実行時エラーとする。
- `num`へ数値を渡した場合は、その数値をそのまま返す。
- `num`へ数値として解釈できる文字列を渡した場合は数値値を返す。
- 空文字や数値として解釈できない文字列、日時値を`num`へ渡した場合は実行時エラーと
  する。
- `num`が受け付ける文字列表現は、choの数値リテラルと同じ規則を基本とし、実装前に
  境界値と指数表記の扱いを確認する。

この規則は、入力の列や変換式を取り違えたプログラムを、比較不成立として黙って通さ
ないためのものとする。型の間違いと変換できない入力は、そのレコードで明示的に失敗
させる。

## 初期バージョンの仕様

### 日時値

- 内部ではUTCの日時として保持する。
- RFC 3339入力に含まれるオフセットは保持せず、同じ瞬間のUTCへ正規化する。
- 日時値を文字列として評価するとRFC 3339形式を返す。
- UTCの表記には`+00:00`ではなく`Z`を使う。
- 小数秒は、値に存在する場合だけ出力する方針を基本とする。

例：

```text
2026-08-18T12:34:56+09:00
    -> 2026-08-18T03:34:56Z
```

### RFC 3339からの生成

```lisp
(dt VALUE)
```

- RFC 3339として妥当な文字列だけを受け付ける。
- `2026-08-18`のような日付だけの値は、初期バージョンでは受け付けない。
- 曖昧な日付形式や独自の自動判定は行わない。
- 入力タイムゾーンの省略を許さない。

### Unix timestampからの生成

```lisp
(dt/unix VALUE)
```

- Unix epochからの秒数として解釈する。
- ミリ秒か秒かを桁数で自動判定しない。
- 数値でない値や、表現可能範囲を外れた値は実行時エラーとする。
- 負のUnix秒を許可するか、小数秒を許可するかは実装前に決定する。

### 任意形式での表示

```lisp
(dt/fmt "%Y/%m/%d" (dt $1))
```

- strftime互換の形式を使う想定とする。
- 結果は日時値ではなく文字列値とする。
- `VALUE`が日時値なら、そのまま書式化する。
- `VALUE`が文字列なら、RFC 3339として直接パースしてから書式化する。
- したがって、表示だけが目的なら`dt`を明示しなくてよい。

```lisp
(dt/fmt "%Y/%m/%d" $1)
```

- Unix秒は暗黙に判定しない。Unix秒を表示する場合は`dt/unix`を明示する。

```lisp
(dt/fmt "%Y/%m/%d" (dt/unix $1))
```

- `VALUE`が空文字または不正なRFC 3339文字列の場合は実行時エラーとする。
- `FORMAT`を通常の値式として扱うか、文字列リテラルだけに制限するかを実装前に
  決定する。
- 不正な書式指定をプログラムエラーと実行時エラーのどちらにするかも、上記の決定に
  合わせる。

この規則により、`dt`は`dt/fmt`の必須の前処理ではなく、次の用途を持つ。

- RFC 3339入力をUTCのRFC 3339へ正規化する。
- 日時値を`if`などの値式の間で保持する。
- 既存の比較述語で時系列を比較する。

### 日時値の比較

既存の比較述語を日時値へ対応させ、日時専用の比較構文は追加しない。

```lisp
(filter (>= (dt $1) (dt "2026-08-01T00:00:00Z")))
```

- 日時値同士の`<`、`<=`、`>`、`>=`は時間順で比較する。
- 日時値同士の`=`は、表記やUTCオフセットが異なっても同じ瞬間なら真とする。
- 日時値同士の`!=`は、同じ瞬間でなければ真とする。
- 日時値と文字列、または日時値と数値を比較した場合は、上記の厳密な型比較に従って
  実行時エラーとする。
- 日時として比較する両辺を、`dt`または`dt/unix`で明示的に日時値へ変換する。
- 日時へ変換していないフィールドは従来どおり文字列値であり、文字列として比較する。

```lisp
(filter
  (=
    (dt "2026-08-18T12:00:00+09:00")
    (dt "2026-08-18T03:00:00Z")))
```

上記の比較は真になる。

## 実行時エラー

式の構文が不正な場合と、入力レコードの変換に失敗した場合を分ける。

- 引数不足、余分な引数、不正な式は、従来どおり不正なプログラムとして終了する。
- 空文字、日付だけの値、不正なRFC 3339文字列を`dt`へ渡した場合は実行時エラーとする。
- 数値でない値または表現可能範囲を外れた値を`dt/unix`へ渡した場合も実行時エラーと
  する。
- RFC 3339としてパースできない文字列を`dt/fmt`へ直接渡した場合も実行時エラーとする。
- `num`で数値へ変換できない値は実行時エラーとする。
- 異なる型の値を比較した場合は、演算子にかかわらず実行時エラーとする。

実行時エラーが発生した時点で入力処理を停止し、診断を標準エラーへ出力して非0の終了
コードで終了する。それ以前のレコードによる標準出力は取り消さず、そのまま残す。
診断には少なくともレコード番号とエラーの種類を含める。型の混在比較では演算子と
左右の型も示す。

```text
cho: record 12: cannot compare DateTime with String using >
cho: record 15: invalid RFC 3339 datetime
```

## 実装方針

### 1. 依存ライブラリを選定する

- RFC 3339のパース、UTCへの変換、Unix秒からの生成、strftime形式の表示を提供する
  Rustライブラリを選ぶ。
- 第一候補は`chrono`とする。
- 依存追加前に、必要機能だけで構成できるfeature設定を確認する。

### 2. ASTを追加する

数値変換と日時関連の値式を`Value`へ明示的に追加する。

```rust
Value::ParseNumber(Box<Value>)
Value::ParseDateTime(Box<Value>)
Value::DateTimeFromUnix(Box<Value>)
Value::FormatDateTime {
    format: Box<Value>,
    value: Box<Value>,
}
```

実際のvariant名は、ユーザー向け構文名とは独立に意味が明確な名前を使う。

### 3. 実行時の値を型付きにする

現在の評価器はすべての値式を即座に`String`へ変換している。日時値を式の間で保持
できるよう、評価結果を型付きの値へ変更する。

```rust
enum RuntimeValue {
    String(String),
    Number(f64),
    DateTime(DateTime<Utc>),
}
```

この変更では、既存機能の型変換規則を明示する。

- `print`、`str`、`join`など文字列表現が必要な場所では日時値をRFC 3339にする。
- `default`は既存どおり空文字を空として扱う。
- 日時値同士は時間順または同じ瞬間かどうかで比較する。
- 同じ型同士だけを比較し、文字列、数値、日時値の混在比較は実行時エラーにする。
- `if`は選ばれた枝だけを評価し、日時値を文字列化せずそのまま返せるようにする。
- `lower`、`upper`、`count`、`escape`へ日時値を渡した場合の規則を決定してテストする。
  基本案は、既定のRFC 3339文字列表現へ変換してから処理することとする。

### 4. パーサーを拡張する

- `num`と3つの日時式の引数を、既存の再帰的な値文法で解析する。
- 引数不足と余分な引数を拒否する。
- `if`、`default`、`str`、`join`などの内側と外側へネストできるようにする。
- フォーマットをリテラルに限定する場合は、パーサーで明示的に検査する。

### 5. 評価器を実装する

- RFC 3339文字列をパースしてUTCへ正規化する。
- Unix秒からUTC日時を安全に生成し、範囲外でパニックしないようにする。
- 日時値の既定のRFC 3339レンダリングを一か所へ集約する。
- `dt/fmt`で書式化した結果は文字列値として返す。
- `dt/fmt`へ文字列値が渡された場合はRFC 3339として直接パースする。
- `dt/fmt`はUnix秒を暗黙に解釈しない。
- `num`は文字列を数値へ変換し、数値値はそのまま返す。
- 数値・日時変換と型付き比較は実行時エラーを返せる評価結果にし、エラーをCLIまで
  伝播する。
- CLIは実行時エラーで入力処理を止め、標準エラーへ診断を出して非0で終了する。

### 6. テストを追加する

パーサーテスト：

- `num`の正常なASTと他の値式とのネスト
- 各日時式の正常なAST
- 他の値式との多段ネスト
- 引数がゼロの場合
- 引数が不足する場合
- 引数が余る場合
- 不正なフォーマット指定

ランタイムテスト：

- 整数、小数、負数の文字列を`num`で数値へ変換するケース
- 数値値を`num`へ渡すケース
- 空文字、非数値、日時値を`num`へ渡して実行時エラーになること
- 文字列同士、数値同士、日時値同士の比較
- 文字列、数値、日時値のすべての混在比較で実行時エラーになること
- `Z`付きRFC 3339のパース
- 正と負のUTCオフセットからUTCへの正規化
- 小数秒の扱い
- Unix epoch、epoch以前、表現可能範囲の境界
- 日時値をそのまま`print`した場合のRFC 3339出力
- `dt/fmt`による代表的な表示
- RFC 3339文字列を`dt/fmt`へ直接渡すケース
- `dt/unix`で作った日時値を`dt/fmt`へ渡すケース
- 空文字、不正な日時、日付だけの入力で実行時エラーになること
- 数値でないUnix秒と範囲外のUnix秒で実行時エラーになること
- `if`の枝から日時値を返すケース
- `str`と`join`へ日時値を渡すケース
- UTCオフセット表記が異なる同一時刻の等値比較
- 日時値のすべての順序比較
- 実行時エラーより前の標準出力は残ること
- 診断が標準エラーへ出力され、終了コードが非0になること
- RFC 3339形式で出力した複数レコードが`sort`で時系列順になる例

### 7. ヘルプを更新する

- `cho --help`に`num`と3つの日時式、それぞれの小さな例を追加する。
- 既存のhelp、README、テストにある暗黙の数値比較を`num`による明示変換へ更新する。
- RFC 3339とISO 8601を混同しない表現にする。
- 日付だけの入力、曖昧な形式、ミリ秒自動判定をサポートしないことを明記する。
- 数値・日時の変換失敗と型の混在比較が実行時エラーになることを明記する。
- `cho | sort`の組み合わせを代表例として載せる。

## 将来の日時拡張

初期バージョンで日時値と比較規則を固めた後、現在日時と日時計算を値式として追加する。
日時計算には`dur`名前空間の期間値を使う。期間を作る式で単位を明示し、日時側の
加減算は単位によらない少数の式にする。

```lisp
(dur/s NUMBER)             ; 秒単位の期間値
(dur/m NUMBER)             ; 分単位の期間値
(dur/h NUMBER)             ; 時間単位の期間値
(dur/fmt FORMAT DURATION)  ; 期間値を指定形式の文字列にする
(dt/now)                   ; choの起動時点のUTC日時値
(dt/add DATETIME DURATION) ; 日時に期間を加える
(dt/sub DATETIME DURATION) ; 日時から期間を引く
(dt/diff LEFT RIGHT)       ; 2つの日時の差を期間値として得る
```

たとえば、入力された日時の10秒後は次のように書く。

```lisp
(dt/add (dt $1) (dur/s 10))
```

期間値をそのまま`print`、`str`、`join`などで文字列化した場合は、単位記号を付けず
秒数だけを出力する。これにより`dt/diff`の結果も、そのまま出力すれば秒数になる。

```lisp
(print (dur/h 1))
; 3600
```

人が読むための単位や区切りを含む表示が必要な場合は、`dur/fmt`で明示的に文字列へ
変換する。`dur/print`のような出力専用式は追加せず、既存の`print`へ値式をネストする。

```lisp
(print (dur/fmt FORMAT (dt/diff (dt $1) (dt $2))))
```

`FORMAT`の具体的な構文、負の期間と小数秒の表示規則は実装前に決める。暦上の月や年は
長さが一定でないため、固定時間の期間値と同じものとして暗黙に扱わない。

`dt/now`はレコードごとに時計を読み直さず、1回のcho実行中では同じ日時値を返す。
これにより、大きな入力の途中で判定基準がずれることを避け、次のような比較を一貫して
評価できるようにする。

```lisp
(filter (<= (dt $1) (dt/now)))
```

日時同士の比較には、初期バージョンで対応する既存の`<`、`<=`、`>`、`>=`、`=`、
`!=`をそのまま使い、日時専用の比較式は追加しない。

## 将来の言語拡張

### スレッディング

値式を深くネストせず左から右へ読めるよう、Clojureの`->`と`->>`に相当する
スレッディング構文を追加する。

`->`は直前の値を各式の先頭引数へ差し込む。

```lisp
(-> $1
    (dt)
    (dt/add (dur/s 10)))
```

上記は次の式と同じ意味になる。

```lisp
(dt/add (dt $1) (dur/s 10))
```

`->>`は直前の値を各式の末尾引数へ差し込む。

```lisp
(->> $1
     (dt/fmt "%Y/%m/%d")
     (str "date: "))
```

上記は次の式と同じ意味になる。

```lisp
(str "date: " (dt/fmt "%Y/%m/%d" $1))
```

初期実装では値式だけを対象とし、一般的なマクロ定義機能は追加しない。展開後の式が
既存の値文法として不正なら、従来どおり不正なプログラムとして拒否する。ASTへ専用の
表現を残すか、パース時に既存の値式へ展開するかは実装時に決める。

テストでは、引数なしのステップ、複数段の展開、ネストしたスレッディング、展開後の
引数不足と余分な引数を確認する。`cho --help`には`->`と`->>`の展開前後を並べた短い
例を載せる。

## 初期バージョンでは扱わないもの

- 日付だけを表す別の型
- RFC 3339以外の日付・日時形式の自動判定
- ローカルタイムゾーンの暗黙利用
- タイムゾーン名やロケール
- 上記の将来の日時拡張
- 上記のスレッディング構文
- 日時からUnix timestampへの逆変換
- Unixミリ秒・マイクロ秒・ナノ秒の自動判定
- cho内部でのソート
- `BEGIN`、`END`、レコードをまたぐ状態

## 実装前に決める事項

- Unix秒で負数を許可するか
- Unix秒で小数を許可するか。許可する場合の精度と丸め規則
- 日時値の小数秒を既定出力でどのように保持するか
- `dt/fmt`の`FORMAT`を値式にするか文字列リテラルに限定するか
- 不正な日時フォーマットをプログラムエラーと実行時エラーのどちらにするか
- 日時値を`lower`、`upper`、`count`、`escape`へ渡した場合の確定仕様

## 完了条件

```console
$ cargo fmt --check
$ cargo test
$ cargo clippy --all-targets -- -D warnings
```

上記に加え、実際のバイナリで以下を確認する。

- RFC 3339入力がUTCのRFC 3339へ正規化される。
- Unix秒が同じ日時値へ変換される。
- 任意形式で表示できる。
- `num`で明示的に変換した値を数値比較できる。
- 文字列、数値、日時値の混在比較で標準エラーへ診断を出し、非0で終了する。
- 不正な日時で標準エラーへ診断を出し、非0の終了コードで終了する。
- 実行時エラーより前に出力したレコードは標準出力に残る。
- 既定出力を`sort -k`へ渡して時系列順に並べられる。
