# TODO

choは、小さく組み合わせやすい、型を意識したコマンドラインのテキスト処理系を目指す。

```text
cho is a small, type-aware text processor for the command line.
```

文字列、数値、日時、期間、IPアドレス、CIDRなどを、各式のシグネチャが要求する型へ
文脈的に変換する。入力のたびに明示的な型変換を書かせず、変換できない入力は
レコード番号、式名、引数位置、期待型を含む実行時エラーにする。

### 現在の主な規則

- 入力フィールドはStringであり、式が要求した場合だけNumber、DateTime、Duration、
  IpAddr、Cidrへ変換する。
- `=`、`!=`、`<`、`<=`、`>`、`>=`はNumberを要求する。
- 文字列比較は`s/`、日時比較は`dt/`、IP等値比較は`ip/`を使う。
- DateTime入力はタイムゾーン付きRFC 3339に限定し、既定表示はUTCへ正規化する。
- `dt/fmt`のタイムゾーンはIANA名または`±HH:MM`で局所的に明示し、実行環境の
  ローカルタイムゾーンは暗黙に使わない。
- Durationの既定表示は単位なしの秒数とする。
- `dt/diff LEFT RIGHT`は`LEFT - RIGHT`を返す。
- `ip/private?`はRFC 1918のIPv4とIPv6 ULA `fc00::/7`を真とする。
- 異なるアドレスファミリーの`cidr/contains?`はfalseとする。
- 実行時エラーより前の標準出力は残し、最後は診断と非0終了コードで終える。

## 次に検討するもの

### フィールド範囲

連続するフィールドを単一のString値として取り出す構文を検討する。

```lisp
$-3
$3-
$2-4
```

それぞれ先頭から3番目、3番目から末尾、2番目から4番目までを表す。範囲は値式として
`s/upper`、`s/count`、`default`などへネストできる形にする。

特に、ログのメッセージ部分を取り出す`$3-`では、次の結果が期待される。

```console
$ printf '2026-08-24 INFO service started successfully\n' | cho '(p $3-)'
service started successfully
```

単に`(str $3 $4 ... $NF)`へ展開するとフィールド間の空白が消えるため、実装前に
区切りの意味を決める。

決める必要がある事項：

- 既定の空白区切りで、元の連続した空白を保持するか、単一の空白へ正規化するか
- `-F REGEX`で、実際にマッチした区切りを保持するか
- TSVでタブを保持するか
- CSVで元の引用を含む断片を返すか、デコードしたフィールドを再びCSVとして結合するか
- 空レコード、空フィールド、範囲外、逆順、`$0`を含む範囲の扱い

どの入力形式でも、通常の`$N`が返す値との一貫性を優先する。

実装案：

- ASTには範囲の開始と終了を明示した値を持たせる。プログラムのパース後に範囲値の
  有無を判定し、必要なレコードでフィールドの位置情報を構築する。
- 各フィールドについて、値だけでなく元レコード上のbyte単位の`start`と`end`を
  保持する。UTF-8の文字境界を壊さない位置だけを記録する。
- `$2-4`は、2番目のフィールドの`start`から4番目のフィールドの`end`までを元レコード
  から切り出す。フィールド間に実際に存在した空白、タブ、正規表現にマッチした区切りを
  そのまま保持する。
- `$-3`も同様に1番目の`start`から3番目の`end`まで、`$3-`は3番目の`start`から
  最終フィールドの`end`までを切り出す。選択範囲より外側の先頭・末尾の区切りは
  含めない。
- 空フィールドは`start == end`の位置として表現し、範囲内にある区切りを失わない
  ようにする。
- 位置情報を範囲構文がある場合だけ作るか、通常の`$N`も含めてレコードごとに一度だけ
  分割して共有するかは、実装の単純さと計測結果で決める。

CSVでは、元レコードのspanを返すと引用符や`""`を含む一方、通常の`$N`はデコード済みの
値を返すため、`$N-N`と`$N`が一致しなくなる。CSVで範囲構文を許可するか、デコード後の
値を再構成するかは、他の入力形式とは分けて設計する。

### Durationの任意表示

Durationはそのまま出力すれば秒数になる。人向けの表示が本当に必要な実例を集めてから、
`du/fmt`の書式を決める。

```lisp
(du/fmt FORMAT (dt/diff $1 $2))
```

決める必要がある事項：

- 時分秒の各要素と総時間をどう区別するか
- 24時間を超える期間と負の期間の表示
- 小数秒の精度と末尾ゼロ
- FORMATを自由な書式文字列にするか、少数の名前付き形式にするか

### IP分類述語

実際の用途を確認し、必要なものだけを追加する。

- `ip/v4?`
- `ip/v6?`

IPv6 unique-localは、現在の`ip/private?`で扱う。独立した`ip/unique-local?`は、
privateと区別する実例が集まるまで追加しない。

順序比較は当面追加しない。

### その他の型付き値

ログや運用データによく現れ、文字列のままでは正しく扱いにくい値を候補とする。
汎用的なデータ加工機能を増やすのではなく、値の意味を保ったまま短い式で扱えることを
choの強みにする。

優先して検討する候補：

- ByteSize：`10MiB`と`500KB`の比較、単位変換
- UUID：検証、正規化、等値比較
- MACアドレス：表記揺れを吸収した等値比較
- Date：時刻を伴わない日付の比較
- JSON：ログフィールド内の値の抽出。ただし言語規模への影響を慎重に見る

```lisp
(bs/> $1 "100MiB")
(sv/>= $2 "2.4.0")
(date/> $1 "2026-08-01")
(url/host $1)
```

各型は、次を満たすか検討してから一つずつ実装する。

- 文字列比較では意味的に誤る、またはシェルだけで正しく扱うのが難しい
- 検証だけでなく、比較、抽出、変換など複数の有用な操作を定義できる
- 文脈変換によって記述が明確に短くなる
- 外部I/Oや変更可能なグローバル状態を必要とせず、値式として合成できる
- 型名、式名、既定表示、失敗時の診断を`cho --help`で把握できる

各型について、正規化、既定表示、比較、失敗時の診断、最小の型固有操作を決める。
文字列の見た目から型を自動判定せず、`bs/`、`sv/`、`date/`、`url/`など各名前空間の
式が期待型を決める現在の規則を維持する。

### レコード内の型変換キャッシュ

同じ値式をURL、DateTime、IPアドレス、CIDR、SemVerなどへ複数回変換するプログラムでは、
現在は各操作がそれぞれ文字列を解析する。実測で型変換の負荷が問題になった場合は、
プログラムのコンパイル時に同じ値式への同じ型変換が複数あるか判定し、必要な場合だけ
キャッシュスロットを割り当てる。

```lisp
(print (url/host $1) (url/path $1) (url/port $1))
```

キャッシュはレコード単位で作成してレコード処理後に廃棄する。ASTが同一の値式だけを
共有対象とし、同じ型変換を一度しか行わないプログラムにはキャッシュを確保しない。
特定の型だけの仕組みにせず、型変換の共通基盤として設計する。

## 当面扱わないもの

- 曖昧な日付形式やローカルタイムゾーンの暗黙利用
- Unix秒・ミリ秒・マイクロ秒・ナノ秒の桁数による自動判定
- cho内部でのソート
- 一般的なマクロ定義
- `BEGIN`、`END`、レコードをまたぐ変更可能な状態

## 変更時の確認

```console
cargo fmt --check
cargo test
cargo clippy --all-targets -- -D warnings
```

ユーザー向け構文を変更した場合は、実バイナリの出力、標準エラー、終了コードを確認し、
`cho --help`、README、対応するexample、skillのワークフローが矛盾しないことも確認する。
