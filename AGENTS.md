# 開発ガイドライン

## 設計思想

choは、小さく、組み合わせやすく、覚えやすいツールであり続けること。
用途ごとのコマンドやオプションを増やすより、少数の直交した式を一貫した規則で
組み合わせられる設計を優先する。

choは、型を意識したコマンドラインのテキスト処理系である。文字列、数値、日時、期間、
IPアドレス、CIDR、URL、SemVerなどを、各関数のシグネチャが要求する型へ文脈的に
変換する。入力のたびに明示的な型変換を書かせず、変換できない入力はレコード番号、
関数名、引数位置、期待型を含む実行時エラーにする。

- `cho --help`を見れば、その場で目的のワンライナーを書けることを目指す。式名は
  短くても動作と単位を推測できるものにし、helpにはコピーして試せる小さな例を載せる。
  READMEは便利さがすぐ伝わる入口に留め、完全な構文はhelpだけで把握できるようにする。
- 値を変換または結合する機能は、基本的に関数として追加する。`str`、`s/join`、
  `s/count`、`s/escape`のように、値を受け取れる場所ならどこでもネストできる形にする。
- 新しい関数は、既存の式と合成できるようにする。`print`や`filter`の直下でしか
  動かない特例は避ける。
- 変更可能なグローバル状態より、局所的で明示的な式を優先する。たとえば出力の
  区切り文字を設定するオプションや`OFS`変数を追加するより、
  `(print (s/join "," $1 $2))`のほうがchoの設計に合っている。
- `print`は単純に保つ。評価した複数の値を空白で区切って出力し、それ以外の整形は
  式を組み合わせて表現する。
- CLIオプションは、`--csv`、`--tsv`、`-F`のように入力ストリーム全体へ適用する
  入力形式や設定に使う。同じ処理をプログラム内で明快かつ一貫して表現できるなら、
  専用オプションを追加しない。
- 既存のLisp風文法に沿い、シェル固有の複雑なクォートをなるべく必要としない構文を
  選ぶ。
- 配列、ループ、ユーザー定義関数、変数束縛、代入は意図的に言語へ導入しない。choは、
  各レコードを小さな式で処理する、読みやすいワンライナーの範囲に留める。

### 型と実行時の規則

- 入力フィールドはStringであり、関数が要求した場合だけNumber、DateTime、Duration、
  IpAddr、Cidr、Url、SemVerなどに変換する。
- `=`、`!=`、`<`、`<=`、`>`、`>=`はNumberを要求する。
- 文字列比較は`s/`、日時比較は`dt/`などprefixをつける。
- DateTime入力はタイムゾーン付きRFC 3339に限定し、既定表示はUTCへ正規化する。
- `dt/fmt`のタイムゾーンはIANA名または`±HH:MM`で局所的に明示し、実行環境の
  ローカルタイムゾーンは暗黙に使わない。
- Durationの既定表示は単位なしの秒数とする。
- 実行時エラーより前の標準出力は残し、最後は診断と非0終了コードで終える。

### 扱わないもの

- 曖昧な日付形式やローカルタイムゾーンの暗黙利用
- Unix秒・ミリ秒・マイクロ秒・ナノ秒の桁数による自動判定
- cho内部でのソート
- 一般的なマクロ定義
- `BEGIN`、`END`、レコードをまたぐ変更可能な状態
- 特定列を除外する専用の関数や構文
  - 除外時のセパレータ削除などに暗黙のルールが必要になり、直交性や予測可能性を損なうため。
  - 代わりに `(p $..2 $4..)` のように、既存の範囲指定を明示的に組み合わせて抽出する。

## 言語機能を拡張するとき

式を追加するときは、以下を行う。

- AST上の表現を明示的に定義する。
- 他の式と同じ再帰的な構文で引数を解析する。
- 引数がゼロの場合、値が欠けている場合、空文字の場合の挙動を決めてテストする。
- 不正な形式を拒否するパーサーテストを追加する。
- 他の式とネストできることを示すランタイムテストを追加する。
- `cho --help`に構文とコピーして試せる小さな使用例を追加する。
- READMEは機能一覧や詳細仕様の置き場にしない。ツールの便利さを伝える代表例として
  必要な場合だけ更新し、個々の式や境界条件の説明はhelpに置く。

エラー処理は既存の挙動と揃える。不正なプログラムは不正入力として扱い、存在しない
フィールドは空文字として評価する。失敗したfilterは、そのレコードに対する後続の式を
スキップする。

## バージョニング

- 大きな機能追加、ユーザー向け構文の変更、破壊的変更ではマイナーバージョンを上げる。
- 軽微な機能追加、バグ修正、ドキュメント修正ではパッチバージョンを上げる。
- メジャーバージョンは当面上げない。メジャーバージョンを変更する場合は、ユーザーの
  明示的な判断を仰ぐ。

## コミットメッセージ

- コミットメッセージは英語で書く。
- Conventional Commits形式に従い、`feat:`、`fix:`、`refactor:`、`docs:`、`test:`、
  `chore:`など、変更内容に合ったプレフィックスを付ける。

## 動作確認

変更を完了する前に、以下を実行する。

```console
$ cargo fmt --check
$ cargo test
$ cargo clippy --all-targets -- -D warnings
```

ユーザー向けの構文を変更した場合は、代表的なコマンドを実際のバイナリで実行し、
標準出力、標準エラー、終了コードを確認する。`cho --help`、README、対応するexample、
skillのワークフローに矛盾がないことも確認する。
